<div class="">
  <h1 class="font-bold text-4xl">New session</h1>

  <%= render "form", session: @session %>

  <%= link_to 'Back to campaign', campaign_path(@campaign), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
